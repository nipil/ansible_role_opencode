---
- name: Prepare
  hosts: opencode
  gather_facts: false

  pre_tasks:
    - name: Install required packages
      ansible.builtin.apt:
        update_cache: true
        name: openssh-server
        state: present

- name: Import cleanup playbook for actions on localhost
  ansible.builtin.import_playbook: cleanup.yml
